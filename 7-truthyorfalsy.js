//False

Boolean(); //Si no hay valor, por defecto devuelve falso
Boolean(0); //Solamente el 0 es falso
Boolean(null);
Boolean(NaN); //Not a Number
Boolean(undefined);
Boolean(false);
Boolean("");

//True
Boolean(1);
Boolean(" ");
Boolean("a");
Boolean([]);
Boolean({});
Boolean(function(){});
Boolean(true);